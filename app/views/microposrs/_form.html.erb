<%= form_with(model: microposr, local: true) do |form| %>
  <% if microposr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microposr.errors.count, "error") %> prohibited this microposr from being saved:</h2>

      <ul>
      <% microposr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :microposr_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :microposr_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
